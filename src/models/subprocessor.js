export default class Subprocessor {
    constructor(name, purpose, location) {
        this.id = Math.random();
        this.name = name;
        this.purpose = purpose;
        this.location = location;
    }
}